#ifndef ROOM_FOR_SALE_HELPERS_H
#define ROOM_FOR_SALE_HELPERS_H

/**
 * Convert string to int and check if conversion executed correctly
 * @param str string to be converted
 * @param number pointer to int where result should be placed
 * @return is conversion executed correctly
 */
bool strtoint(const char *str, int *number);

#endif //ROOM_FOR_SALE_HELPERS_H
